#include <fstream>
#include <iostream>

using namespace std;

enum Bases { A, C, G, T };

enum Options { USAGE, HUMANTOBIN, BINTOHUMAN };
Options option(int argc, char **argv);

void usage(char *program);
void humanToBin(istream &infile, ostream &outfile);
void binToHuman(istream &infile, ostream &outfile);
