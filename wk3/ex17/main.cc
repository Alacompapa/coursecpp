#include "main.ih"

int main(int argc, char** argv)
{
    string addition;
    for (size_t i = 1; i < argc; ++i)
    {
        string condition = argv[1];
        if(condition.find("."))
        {
            sum(double decimal)
        }
        else{
            sum(int integer)
        }
    }
    cout << addition << "\n";
}
