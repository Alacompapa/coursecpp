#include "main.ih"

using namespace std;

int main(int argc, char** argv)
{
    string addition;
    for (size_t i = 1; i < argc; ++i)
    {
        if(argv[1].find("."))
        {
            sum(double decimal)
        }
        else{
            sum(int integer)
        }
    }
    cout << addition << "\n";
}
