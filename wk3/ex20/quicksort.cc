#include "main.ih"

void quickSort(char** array, size_t leftpos, size_t rightpos) {

    string tmp;
    cout << array;
    // // Partition
    // pivotpos = 0
    // char pivot = array[pivotpos];
    // while (leftpos != rightpos)
    // {
    //     while (array[left][0] < pivot[0])
    //         ++leftpos;
    //
    //     while (array[right][0] > pivot[0])
    //         --rightpos;
    //
    //     if (left <= right)
    //     {
    //         tmp = array[leftpos];
    //         array[leftpos] = array[rightpos];
    //         array[rightpos] = tmp;
    //         ++leftpos;
    //         --rightpos;
    //     }
    // };
    //
    // // Recursion
    // if (array[leftpos] < array[rightpos])
    //     quickSort(array, left, right);
    // if (left < right)
    //     quickSort(array, left, right);
    // else
}
