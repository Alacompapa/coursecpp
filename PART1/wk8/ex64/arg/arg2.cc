#include "arg.ih"

Arg &Arg::initialize(char const *optstring, Arg::LongOption const *const begin, 
                     LongOption const *const end, int argc, char **argv)
{
    // if (not yet init)
    //  return construct Arg (as a ref)
    // 
    // otherwise exit()
}
